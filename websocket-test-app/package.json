{
  "name": "websocket-test-app",
  "version": "1.0.0",
  "description": "A simple WebSocket test application using TypeScript.",
  "main": "dist/app.js",
  "scripts": {
    "start": "python3 -m http.server 8081 --directory dist",
    "dev": "npx tsc && cp ./index.html ./dist/ && python3 -m http.server 8081 --directory dist",
    "build": "npx tsc && cp ./index.html ./dist/"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^4.0.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}